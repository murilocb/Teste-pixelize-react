# teste-pixelize-react

# Iniciar o projeto

O front-end foi utilizado node.js v20 LTS, React e TailwindCSS.

Para instalar as depencencias utilize `npm install`.

E para rodar o projeto em sua maquina `npm run dev`.
